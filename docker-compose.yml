# Local MongoDB only. From repo root: docker compose up -d
services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - fleet_mongo_data:/data/db
    restart: unless-stopped

volumes:
  fleet_mongo_data:

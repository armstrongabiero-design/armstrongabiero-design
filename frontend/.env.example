# Copy to .env in this directory (Create React App reads REACT_APP_* from .env).
# cp .env.example .env

# Backend base URL (no trailing slash). Must match CORS on the API.
REACT_APP_BACKEND_URL=http://127.0.0.1:8000
